// TODO: Implement user model
// Methods to implement:
// - createUser(name, email, passwordHash) → INSERT into users
// - findByEmail(email)                   → SELECT by email
// - findById(id)                         → SELECT by id

const { pool } = require('../config/db.config');

module.exports = {
  // TODO: createUser
  // TODO: findByEmail
  // TODO: findById
};
