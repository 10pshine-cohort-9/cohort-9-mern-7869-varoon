// TODO: Implement note model
// Methods to implement:
// - create(userId, title, content)       → INSERT into notes
// - findAllByUser(userId)                → SELECT all notes for a user
// - findById(id)                         → SELECT note by id
// - update(id, title, content)           → UPDATE note
// - remove(id)                           → DELETE note

const { pool } = require('../config/db.config');

module.exports = {
  // TODO: create
  // TODO: findAllByUser
  // TODO: findById
  // TODO: update
  // TODO: remove
};
